# Linux daemon

![screenshot](daemon_screenshot.png)

# Usage

1) Compile daemon.c:

        g++ daemon.c -o daemon

2) Replace "test" with the Folder/File you want to run the daemon on:

        ./daemon test
